{{ form_start(form, { 'action': path(route, route_args), 'attr': { 'class': 'contact' } }) }}
    {{ form_errors(form) }}

    {{ form_row(form.sender_name) }}
    {{ form_row(form.sender_mail) }}
    {{ form_row(form.subject) }}
    {{ form_row(form.content) }}

    {{ form_rest(form) }}

    <input type="submit" id="kphoen_contact_submit" value="{% trans from "KPhoenContactBundle" %}contact.submit.button{% endtrans %}" />
{{ form_end(form) }}
